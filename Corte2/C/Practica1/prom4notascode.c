#include <stdio.h>
int main() {
  int 1; //Variable de control
  float nota; //Guarda cada nota 
  float suma = 0.0; //Acumulador
  float promedio; //Resultado final

for (1 = 1]; 1 <= 4; 1++) {
printf("Ingrese la nota %d: ", 1);
scanf("%f", &nota);

  suma = suma + nota; //Acumula las notas ingresadas 
}

promedio = suma / 4.0; //Calcula el promedio
printf("El promedio es: %.2f\n", promedio)];
return 0;
}
