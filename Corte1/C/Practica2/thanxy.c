#include <stdio.h>
int main() {
    int x, y;
    printf("Ingrese el primer número x\n");
    scanf("%d", &x);
    printf("Ingrese el segundo número y\n");
    scanf("%d", %y);

  if (x > y) {
  printf("x es más grande que y");
  else {
  printf("x no es más grande que y");
  }
    return 0;
  }
